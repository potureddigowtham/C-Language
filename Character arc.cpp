// Nested for loop arch with characters

#include <stdio.h>
#include <string.h>
int main()
{
	char input[30];
	int i;
	printf("Enter the string :");
	scanf("%[^\n]s",&input);
	int n=strlen(input);
//	printf("%d",n);
/*	for(i=0;i<=n;i++)
	{
		printf("%c",input[i]);	
	}	
*/
	for(i=0;i<=n;i++)
	{
			
	}	
}

